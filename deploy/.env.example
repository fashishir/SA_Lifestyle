# Copy to deploy/.env and edit. Variables not set here use the defaults in docker-compose.yml.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin
POSTGRES_DB=sa_lifestyle
POSTGRES_PORT=5432

JWT_SECRET=change-me-to-a-long-random-string
